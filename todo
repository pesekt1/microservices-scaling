- Implement volumes for all microservices source code, so that we dont need to rebuild the image everytime we make a change when running docker-compoase.
- system test for ci-cd-transaction-receiver is not working. The fraud-detection microservice is not publishing the message on one of the 2 queues.